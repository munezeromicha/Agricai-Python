# Vision API (PM2 / uvicorn on :8000). Platform API (PM2 / Node on :3008).
# Merge into your main Caddyfile or: caddy reload --config /path/to/Caddyfile

ai.agric-ai.com {
	reverse_proxy 127.0.0.1:8000
}

api.agric-ai.com {
	reverse_proxy 127.0.0.1:3008
}